<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".ExampleApplication"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FireworkExample">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".discovery.DiscoveryActivity" />

        <activity android:name=".playlist.PlaylistActivity" />

        <activity android:name=".channel.ChannelActivity" />

        <activity android:name=".dynamiccontent.DynamicContentActivity" />

        <activity android:name=".channelhashtags.ChannelHashtagsActivity" />

        <activity android:name=".sku.SkuActivity" />

        <activity android:name=".singlecontent.SingleContentActivity" />

    </application>

</manifest>
