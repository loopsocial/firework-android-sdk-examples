<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:name="tv.fw.example.ExampleApplication"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FireworkExample">

        <activity
            android:name="tv.fw.example.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="tv.fw.example.discovery.DiscoveryActivity" />

        <activity android:name="tv.fw.example.playlist.PlaylistActivity" />

        <activity android:name="tv.fw.example.channel.ChannelActivity" />

        <activity android:name="tv.fw.example.dynamiccontent.DynamicContentActivity" />

    </application>

</manifest>
